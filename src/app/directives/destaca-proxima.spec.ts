import { DestacaProxima } from './destaca-proxima';

describe('DestacaProxima', () => {
  it('should create an instance', () => {
    const directive = new DestacaProxima();
    expect(directive).toBeTruthy();
  });
});
